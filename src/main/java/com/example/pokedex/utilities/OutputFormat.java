package com.example.pokedex.utilities;

/**
 * Enumeration representing different output formats for Pokémon data.
 */
public enum OutputFormat {
    TEXT, // Human-readable text format
    HTML, // HTML format
    CSV   // Comma-separated values format
}
